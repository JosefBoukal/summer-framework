description = 'The Summer Framework Core API'

dependencies {
    compile("org.springframework:spring-context:${springVersion}")

    compileOnly("org.springframework:spring-web:${springVersion}")

    testCompile project(":summer-test-helper")
}
